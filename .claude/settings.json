{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"REMINDER for hand_pose_claude: Before completing this git commit, ensure you have: (1) appended a NEW version entry at the TOP of CHANGELOG.md (most recent first; keep all prior entries intact); (2) bumped VERSION to the next zero-padded four-digit number (e.g. 0001 -> 0002); (3) rewritten README.md to contain ONLY the latest setup/run info (no historical content; supersede previous content). Stage these files before committing. If already done in this turn, proceed.\"}}'"
          },
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"REMINDER for hand_pose_claude: Before completing this git push, ensure the most recent commit already includes: (1) a NEW version entry at the TOP of CHANGELOG.md (most recent first; keep all prior entries intact); (2) VERSION bumped to the next zero-padded four-digit number (e.g. 0001 -> 0002); (3) README.md rewritten to contain ONLY the latest setup/run info (no historical content; supersede previous content). If the last commit is missing any of these, create a follow-up commit before pushing. If already done, proceed.\"}}'"
          }
        ]
      }
    ]
  }
}
